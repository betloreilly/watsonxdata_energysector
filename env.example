# =============================================================================
# OpenSearch (IBM watsonx.data managed - recommended)
# =============================================================================
# Copy this file to .env and set your values. Used by:
#   scripts/sync_to_opensearch.py
#   scripts/generate_sample_data.py
#   scripts/generate_streaming_data.py
#
# For Docker OpenSearch, leave OPENSEARCH_URL unset (scripts use localhost:9200).

OPENSEARCH_URL=https://<your-watsonx-opensearch-endpoint>:9200
OPENSEARCH_USERNAME=your-username
OPENSEARCH_PASSWORD=your-password

# Optional: index name (default: energy-sensor-readings)
# OPENSEARCH_INDEX=energy-sensor-readings

# =============================================================================
# OpenSearch (Docker / local only - when OPENSEARCH_URL is not set)
# =============================================================================
# OPENSEARCH_HOST=localhost
# OPENSEARCH_PORT=9200

# =============================================================================
# Cassandra (for sync_to_opensearch.py)
# =============================================================================
# CASSANDRA_HOST=10.0.1.123
# CASSANDRA_KEYSPACE=energy_ks
# CASSANDRA_TABLE=sensor_readings_by_asset
