<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Set log level for Cassandra driver to WARN (suppress DEBUG messages) -->
    <logger name="com.datastax.oss.driver" level="WARN"/>
    <logger name="com.datastax.dse.driver" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    
    <!-- Set log level for our application to INFO -->
    <logger name="com.ibm.wxd.datalabs.demo" level="INFO"/>
    
    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>

